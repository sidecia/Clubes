<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clubes.imagencentral.clubes" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashInicio"
            android:label="@string/title_activity_splash_inicio" >
        </activity>
        <activity
            android:name=".NoticiaDetalle"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_noticia_detalle" >
        </activity>
        <activity
            android:name=".ActividadDetalleActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_actividad_detalle" >
        </activity>
        <activity
            android:name=".GaleriaCompleta"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_galeria_completa" >
        </activity>
        <activity
            android:name=".CalendarioDetalle"
            android:configChanges="orientation|screenSize"
            android:label="" >
        </activity>
    </application>

</manifest>
